package com.springboot.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import com.springboot.entity.Venta;

public interface VentaRepository extends JpaRepository<Venta, Long> {
}